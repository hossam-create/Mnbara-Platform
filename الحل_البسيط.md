# 🎯 الحل البسيط - استخدام SQLite بدل PostgreSQL

## المشكلة
PostgreSQL محتاج باسورد ومعقد. خلينا نستخدم SQLite - أسهل بكتير!

## ✅ المميزات
- ✅ مافيش باسوردات
- ✅ مافيش setup معقد
- ✅ ملف واحد لكل database
- ✅ يشتغل مباشرة

---

## 🚀 الخطوات (3 دقائق فقط!)

### الخطوة 1️⃣: تحديث Prisma Schema

نفذ السكريبت ده:

```powershell
scripts\convert-to-sqlite.bat
```

### الخطوة 2️⃣: تشغيل Migrations

```powershell
scripts\run-all-migrations-sqlite.bat
```

### الخطوة 3️⃣: تشغيل الخدمات

```powershell
scripts\start-services-sqlite.bat
```

### الخطوة 4️⃣: إنشاء البيانات

```powershell
cd scripts
npm run seed
```

### الخطوة 5️⃣: الاختبار

```powershell
cd scripts
npm test
```

---

## 📁 ملفات SQLite

كل database هيكون ملف في المجلد:
```
backend/services/[service-name]/prisma/dev.db
```

مثال:
- `backend/services/auth-service/prisma/dev.db`
- `backend/services/user-service/prisma/dev.db`
- إلخ...

---

## 🔄 البديل: استخدام Docker PostgreSQL

إذا تفضل PostgreSQL، استخدم Docker:

```powershell
# تشغيل PostgreSQL في Docker
docker run -d --name postgres-local -e POSTGRES_PASSWORD=postgres123 -e POSTGRES_USER=postgres -p 5432:5432 postgres:15-alpine

# انتظر 5 ثواني
timeout /t 5

# إنشاء المستخدم والقواعد
docker exec -it postgres-local psql -U postgres -c "CREATE USER mnbarh WITH PASSWORD 'mnbarh123';"
docker exec -it postgres-local psql -U postgres -c "CREATE DATABASE auth_db OWNER mnbarh;"
docker exec -it postgres-local psql -U postgres -c "CREATE DATABASE user_db OWNER mnbarh;"
# ... باقي القواعد
```

---

## 🎯 أيهما أفضل؟

### SQLite (موصى به للتطوير المحلي):
- ✅ سهل جداً
- ✅ مافيش setup
- ✅ سريع
- ❌ مش للإنتاج

### PostgreSQL (للإنتاج):
- ✅ قوي
- ✅ للإنتاج
- ❌ محتاج setup
- ❌ محتاج باسورد

---

**🚀 يلا نبدأ بـ SQLite!**
