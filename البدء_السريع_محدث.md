# 🚀 البدء السريع - المرحلة 2.5 (محدث)

## ✅ الحالة الحالية
- ✅ Docker يعمل (v29.1.3)
- ✅ Node.js مثبت
- ✅ المكتبات المطلوبة مثبتة (axios, chalk)
- ✅ البنية التحتية تعمل:
  - PostgreSQL (Port 5432) ✅
  - Redis (Port 6379) ✅
  - RabbitMQ (Ports 5672, 15672) ✅

---

## 📝 الخطوات التالية (طريقة محدثة - أسرع!)

### 1️⃣ تشغيل Migrations (3 دقائق)

```bash
scripts\run-all-migrations.bat
```

سيقوم السكريبت بتشغيل migrations لجميع الخدمات تلقائياً:
- Auth Service
- User Service
- Payment Service
- Product Service
- Wallet Service
- Orders Service
- Escrow Service
- Trips Service
- Matching Service
- Notification Service
- Subscription Service

⏱️ **الوقت المتوقع**: 2-3 دقائق

---

### 2️⃣ تشغيل الخدمات الخلفية (60 ثانية)

```bash
scripts\start-services-manual.bat
```

سيتم فتح 11 نافذة منفصلة لكل خدمة. انتظر 60 ثانية حتى تبدأ جميع الخدمات.

⏱️ **الوقت المتوقع**: 60 ثانية للبدء

**ملاحظة**: ستبقى النوافذ مفتوحة لمراقبة السجلات (logs) لكل خدمة.

---

### 3️⃣ إنشاء بيانات الاختبار (30 ثانية)

```bash
cd scripts
npm run seed
```

سيتم إنشاء:
- 5 مشترين (50,000 جنيه لكل واحد)
- 5 بائعين (10,000 جنيه + اشتراك نشط)
- 5 مسافرين (5,000 جنيه لكل واحد)
- 5 منتجات (iPhone, Nike, Sony, Gucci, MacBook)
- 5 رحلات (New York, London, Dubai, Paris, Istanbul → Cairo)

⏱️ **الوقت المتوقع**: 30 ثانية

---

### 4️⃣ تشغيل الاختبارات (3 دقائق)

```bash
cd scripts
npm test
```

سيتم اختبار:
- ✅ Happy Path: طلب كامل من البداية للنهاية
- ✅ Dispute Flow: نزاع وحل
- ✅ Cancellation Flow: إلغاء وإرجاع الأموال

⏱️ **الوقت المتوقع**: 3 دقائق

---

## 🔐 بيانات الدخول للاختبار

### مشتري
- البريد: `buyer1@test.local` إلى `buyer5@test.local`
- كلمة المرور: `Test123!@#`
- الرصيد: 50,000 جنيه

### بائع
- البريد: `seller1@test.local` إلى `seller5@test.local`
- كلمة المرور: `Test123!@#`
- الرصيد: 10,000 جنيه
- الاشتراك: نشط (Premium)

### مسافر
- البريد: `traveler1@test.local` إلى `traveler5@test.local`
- كلمة المرور: `Test123!@#`
- الرصيد: 5,000 جنيه

---

## 📊 روابط الخدمات

### الخدمات الخلفية
- Auth: http://localhost:3001
- User: http://localhost:3002
- Payment: http://localhost:3003
- Product: http://localhost:3004
- Wallet: http://localhost:3005
- Orders: http://localhost:3006
- Escrow: http://localhost:3007
- Trips: http://localhost:3009
- Matching: http://localhost:3010
- Notification: http://localhost:3011
- Subscription: http://localhost:3012

### البنية التحتية
- PostgreSQL: `localhost:5432`
  - المستخدم: `mnbarh_local`
  - كلمة المرور: `local_test_password`
- Redis: `localhost:6379`
- RabbitMQ: http://localhost:15672
  - المستخدم: `guest`
  - كلمة المرور: `guest`

### الواجهة الأمامية (اختياري)
```bash
cd frontend\web-app
npm run dev
```
- تطبيق الويب: http://localhost:5173

---

## 🔧 استكشاف الأخطاء

### خدمة لا تبدأ
1. تحقق من النافذة الخاصة بالخدمة
2. تأكد من أن البنية التحتية تعمل:
```bash
docker-compose -f docker-compose.local-mvp.yml ps
```
3. تحقق من أن Migration تم بنجاح
4. أعد تشغيل الخدمة من نافذتها

### مشكلة في Database
```bash
# فحص اتصال PostgreSQL
docker exec -it mnbarh-postgres-local psql -U mnbarh_local -d auth_db -c "\dt"
```

### إعادة تشغيل البنية التحتية
```bash
# إيقاف
docker-compose -f docker-compose.local-mvp.yml down

# بداية جديدة
docker-compose -f docker-compose.local-mvp.yml up -d postgres redis rabbitmq
```

### تنظيف كامل (تحذير: سيحذف كل البيانات!)
```bash
docker-compose -f docker-compose.local-mvp.yml down -v
docker-compose -f docker-compose.local-mvp.yml up -d postgres redis rabbitmq
# ثم أعد تشغيل migrations
scripts\run-all-migrations.bat
```

---

## ⏱️ الوقت المتوقع (الطريقة الجديدة)

- ✅ البنية التحتية: جاهزة بالفعل
- Migrations: 3 دقائق
- تشغيل الخدمات: 1 دقيقة
- إنشاء البيانات: 0.5 دقيقة
- الاختبارات: 3 دقائق

**المجموع: ~7-8 دقائق فقط!** 🚀

---

## 📝 بعد الانتهاء

1. افتح `PHASE_2_6_VALIDATION_REPORT_TEMPLATE.md`
2. انسخه إلى `PHASE_2_6_VALIDATION_REPORT.md`
3. املأ النتائج من الاختبارات
4. وثق أي مشاكل واجهتها
5. احفظ التقرير

---

## 🎯 السيناريوهات المختبرة

### Happy Path (المسار السعيد)
1. مشتري يسجل دخول
2. يطلب منتج
3. مسافر يقبل الطلب
4. دفع من المشتري
5. تسليم المنتج
6. تأكيد الاستلام
7. إطلاق الأموال للبائع والمسافر

### Dispute Flow (نزاع)
1. مشتري يطلب منتج
2. مسافر يقبل
3. دفع
4. مشتري يفتح نزاع
5. Admin يحل النزاع
6. إرجاع الأموال

### Cancellation Flow (إلغاء)
1. مشتري يطلب منتج
2. مسافر يقبل
3. دفع
4. مشتري يلغي
5. إرجاع الأموال تلقائياً

---

## 🚫 تذكير مهم

- ❌ لا تستخدم بيانات حقيقية
- ❌ لا تستخدم Stripe حقيقي (استخدم TEST mode فقط)
- ❌ لا تنشر على السحابة
- ✅ اختبار محلي فقط
- ✅ جميع البيانات وهمية

---

## 🔄 الفرق عن الطريقة القديمة

### القديمة (20-30 دقيقة):
- Docker build لجميع الخدمات
- انتظار طويل للبناء
- مشاكل في package-lock.json

### الجديدة (7-8 دقائق):
- ✅ تشغيل مباشر بدون build
- ✅ نوافذ منفصلة لكل خدمة
- ✅ سهولة مراقبة السجلات
- ✅ أسرع بكثير!

---

**🔒 وضع الاختبار المحلي فقط - لا بيانات إنتاج**

**🎉 جاهز للبدء؟ ابدأ بالخطوة 1!**
