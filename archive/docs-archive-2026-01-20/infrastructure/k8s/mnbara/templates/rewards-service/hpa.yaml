{{- if and .Values.rewardsService.enabled .Values.rewardsService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.rewardsService.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "mnbara.serviceLabels" (list . .Values.rewardsService.name) | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.rewardsService.name }}
  minReplicas: {{ .Values.rewardsService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.rewardsService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.rewardsService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
