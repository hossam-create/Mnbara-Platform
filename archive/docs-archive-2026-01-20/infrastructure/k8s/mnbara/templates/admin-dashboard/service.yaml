{{- if .Values.adminDashboard.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.adminDashboard.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "mnbara.serviceLabels" (list . .Values.adminDashboard.name) | nindent 4 }}
spec:
  type: {{ .Values.adminDashboard.service.type }}
  ports:
    - port: {{ .Values.adminDashboard.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mnbara.serviceSelectorLabels" (list . .Values.adminDashboard.name) | nindent 4 }}
{{- end }}
