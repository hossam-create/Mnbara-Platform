# 📝 الخطوات المتبقية للإكمال

**الحالة الحالية:** ✅ البنية التحتية تعمل (PostgreSQL, Redis, RabbitMQ)

---

## ⚠️ ملاحظة مهمة

تشغيل جميع الخدمات يتطلب build للـ Docker images، وهذا قد يستغرق **20-30 دقيقة** في المرة الأولى.

**لديك خياران:**

---

## الخيار 1: تشغيل تلقائي كامل (موصى به للمرة الأولى)

### الخطوة 1: تشغيل جميع الخدمات
```bash
docker-compose -f docker-compose.local-mvp.yml up -d --build
```

**الوقت المتوقع:** 20-30 دقيقة (build + start)

### الخطوة 2: مراقبة التقدم
```bash
# في نافذة منفصلة
docker-compose -f docker-compose.local-mvp.yml logs -f
```

### الخطوة 3: التحقق من الحالة
```bash
docker-compose -f docker-compose.local-mvp.yml ps
```

انتظر حتى تصبح جميع الخدمات "Up" و "healthy"

---

## الخيار 2: تشغيل يدوي خطوة بخطوة (للتحكم الأفضل)

### الخطوة 1: تشغيل خدمة واحدة للاختبار
```bash
# مثال: Auth Service
docker-compose -f docker-compose.local-mvp.yml up -d --build auth-service
```

### الخطوة 2: التحقق من نجاحها
```bash
docker-compose -f docker-compose.local-mvp.yml logs auth-service
curl http://localhost:3001/health
```

### الخطوة 3: تشغيل باقي الخدمات تدريجياً
```bash
docker-compose -f docker-compose.local-mvp.yml up -d --build user-service
docker-compose -f docker-compose.local-mvp.yml up -d --build wallet-service
docker-compose -f docker-compose.local-mvp.yml up -d --build payment-service
# ... وهكذا
```

---

## بعد تشغيل الخدمات

### الخطوة 4: تشغيل Migrations

يجب تشغيل migrations لكل خدمة:

```bash
# Auth Service
cd backend/services/auth-service
npx prisma migrate deploy
cd ../../..

# User Service  
cd backend/services/user-service
npx prisma migrate deploy
cd ../../..

# Wallet Service
cd backend/services/wallet-service
npx prisma migrate deploy
cd ../../..

# Payment Service
cd backend/services/payment-service
npx prisma migrate deploy
cd ../../..

# Escrow Service
cd backend/services/escrow-service
npx prisma migrate deploy
cd ../../..

# Orders Service
cd backend/services/orders-service
npx prisma migrate deploy
cd ../../..

# Trips Service
cd backend/services/trips-service
npx prisma migrate deploy
cd ../../..

# Matching Service
cd backend/services/matching-service
npx prisma migrate deploy
cd ../../..

# Notification Service
cd backend/services/notification-service
npx prisma migrate deploy
cd ../../..

# Subscription Service
cd backend/services/subscription-service
npx prisma migrate deploy
cd ../../..
```

**الوقت المتوقع:** 10 دقائق

---

### الخطوة 5: إنشاء بيانات الاختبار

```bash
cd scripts
npm run seed
```

**الوقت المتوقع:** 2 دقيقة

سيتم إنشاء:
- 5 مشترين (50,000 جنيه لكل واحد)
- 5 بائعين (10,000 جنيه + اشتراك نشط)
- 5 مسافرين (5,000 جنيه لكل واحد)
- 5 منتجات
- 5 رحلات

---

### الخطوة 6: تشغيل الاختبارات

```bash
cd scripts
npm test
```

**الوقت المتوقع:** 3 دقائق

---

## 🔧 استكشاف الأخطاء

### إذا فشل build لخدمة معينة:

```bash
# فحص السجلات
docker-compose -f docker-compose.local-mvp.yml logs [service-name]

# إعادة build
docker-compose -f docker-compose.local-mvp.yml build --no-cache [service-name]

# إعادة التشغيل
docker-compose -f docker-compose.local-mvp.yml up -d [service-name]
```

### إذا فشل migration:

```bash
# فحص اتصال قاعدة البيانات
docker exec -it mnbarh-postgres-local psql -U mnbarh_local -d mnbarh_local

# قائمة قواعد البيانات
\l

# الاتصال بقاعدة بيانات محددة
\c auth_db
```

### إذا فشل seeding:

```bash
# فحص أن الخدمات تعمل
curl http://localhost:3001/health  # Auth
curl http://localhost:3002/health  # User
curl http://localhost:3005/health  # Wallet

# تشغيل مع تفاصيل أكثر
cd scripts
node local-mvp-seed-test-data.js
```

---

## 📊 الوقت الإجمالي المتوقع

- تشغيل الخدمات (build): 20-30 دقيقة
- Migrations: 10 دقائق
- Seeding: 2 دقيقة
- الاختبارات: 3 دقيقة

**المجموع: 35-45 دقيقة**

---

## ✅ معايير النجاح

بعد الانتهاء، يجب أن:
- [ ] جميع الخدمات تعمل (docker ps)
- [ ] جميع الخدمات healthy
- [ ] Migrations نجحت
- [ ] 15 مستخدم تم إنشاؤهم
- [ ] الاختبارات نجحت

---

## 📝 التقرير النهائي

بعد الانتهاء:
1. انسخ `PHASE_2_6_VALIDATION_REPORT_TEMPLATE.md`
2. املأ النتائج
3. وثق المشاكل
4. احفظ كـ `PHASE_2_6_VALIDATION_REPORT.md`

---

**🔒 تذكير: وضع الاختبار فقط - لا بيانات إنتاج**

