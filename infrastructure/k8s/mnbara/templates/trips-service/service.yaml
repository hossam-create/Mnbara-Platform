{{- if .Values.tripsService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.tripsService.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "mnbara.serviceLabels" (list . .Values.tripsService.name) | nindent 4 }}
spec:
  type: {{ .Values.tripsService.service.type }}
  ports:
    - port: {{ .Values.tripsService.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mnbara.serviceSelectorLabels" (list . .Values.tripsService.name) | nindent 4 }}
{{- end }}
