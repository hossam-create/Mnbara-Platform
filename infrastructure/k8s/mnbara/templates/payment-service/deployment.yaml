{{- if .Values.paymentService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.paymentService.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "mnbara.serviceLabels" (list . .Values.paymentService.name) | nindent 4 }}
spec:
  {{- if not .Values.paymentService.autoscaling.enabled }}
  replicas: {{ .Values.paymentService.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "mnbara.serviceSelectorLabels" (list . .Values.paymentService.name) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "mnbara.serviceSelectorLabels" (list . .Values.paymentService.name) | nindent 8 }}
    spec:
      serviceAccountName: {{ include "mnbara.serviceAccountName" . }}
      containers:
        - name: {{ .Values.paymentService.name }}
          image: {{ include "mnbara.image" (dict "root" . "service" .Values.paymentService) }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.paymentService.service.port }}
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.paymentService.env.PORT | quote }}
            - name: NODE_ENV
              value: {{ .Values.paymentService.env.NODE_ENV | quote }}
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ default (printf "%s-database" (include "mnbara.fullname" .)) .Values.secrets.database.existingSecret }}
                  key: password
            - name: DATABASE_URL
              value: {{ include "mnbara.databaseUrl" . | quote }}
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ default (printf "%s-redis" (include "mnbara.fullname" .)) .Values.secrets.redis.existingSecret }}
                  key: redis-password
            - name: REDIS_URL
              value: {{ include "mnbara.redisUrl" . | quote }}
            - name: STRIPE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ default (printf "%s-stripe" (include "mnbara.fullname" .)) .Values.secrets.stripe.existingSecret }}
                  key: secret-key
            - name: STRIPE_WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ default (printf "%s-stripe" (include "mnbara.fullname" .)) .Values.secrets.stripe.existingSecret }}
                  key: webhook-secret
            - name: PAYPAL_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: {{ default (printf "%s-paypal" (include "mnbara.fullname" .)) .Values.secrets.paypal.existingSecret }}
                  key: client-id
            - name: PAYPAL_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ default (printf "%s-paypal" (include "mnbara.fullname" .)) .Values.secrets.paypal.existingSecret }}
                  key: secret
          envFrom:
            - configMapRef:
                name: {{ include "mnbara.fullname" . }}-common
          resources:
            {{- toYaml .Values.paymentService.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
{{- end }}
