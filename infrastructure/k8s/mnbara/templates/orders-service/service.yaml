{{- if .Values.ordersService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.ordersService.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "mnbara.serviceLabels" (list . .Values.ordersService.name) | nindent 4 }}
spec:
  type: {{ .Values.ordersService.service.type }}
  ports:
    - port: {{ .Values.ordersService.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mnbara.serviceSelectorLabels" (list . .Values.ordersService.name) | nindent 4 }}
{{- end }}
