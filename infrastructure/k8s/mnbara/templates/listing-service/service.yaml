{{- if .Values.listingService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.listingService.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "mnbara.serviceLabels" (list . .Values.listingService.name) | nindent 4 }}
spec:
  type: {{ .Values.listingService.service.type }}
  ports:
    - port: {{ .Values.listingService.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mnbara.serviceSelectorLabels" (list . .Values.listingService.name) | nindent 4 }}
{{- end }}
