{{- if .Values.matchingService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.matchingService.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "mnbara.serviceLabels" (list . .Values.matchingService.name) | nindent 4 }}
spec:
  type: {{ .Values.matchingService.service.type }}
  ports:
    - port: {{ .Values.matchingService.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mnbara.serviceSelectorLabels" (list . .Values.matchingService.name) | nindent 4 }}
{{- end }}
