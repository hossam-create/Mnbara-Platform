/**
 * Jest Setup File
 * Runs before all tests
 */

// Set test timeout
jest.setTimeout(10000);

// Suppress console output during tests if needed
// global.console = {
//   ...console,
//   log: jest.fn(),
//   debug: jest.fn(),
//   info: jest.fn(),
//   warn: jest.fn(),
//   error: jest.fn(),
// };
