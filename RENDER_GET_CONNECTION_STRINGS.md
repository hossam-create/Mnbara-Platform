# 🔗 كيفية الحصول على DATABASE_URL و REDIS_URL من Render

**دليل مصور خطوة بخطوة**

---

## 📊 الفرق بين الـ URLs

| النوع | الاستخدام | الشكل |
|------|----------|-------|
| **Internal URL** | للخدمات داخل Render | `postgresql://...@dpg-xxxxx.render.internal:5432/...` |
| **External URL** | للاتصال من خارج Render | `postgresql://...@dpg-xxxxx.c.render.com:5432/...` |

**نحتاج:** Internal URL (لأن الخدمات على نفس Render)

---

## 🗄️ الحصول على DATABASE_URL من PostgreSQL

### الطريقة 1: عبر لوحة التحكم (الأسهل)

1. اذهب إلى [Render Dashboard](https://dashboard.render.com)
2. اضغط على **"Databases"** في القائمة اليسرى
3. اختر قاعدة البيانات `mnbara-db`
4. ستجد صفحة تحتوي على معلومات الاتصال
5. ابحث عن **"Internal Database URL"** أو **"Connection String"**
6. انسخ الـ URL كاملاً

**الشكل:**
```
postgresql://mnbara_user:PASSWORD@dpg-xxxxx.render.internal:5432/mnbara_prod
```

### الطريقة 2: عبر Render CLI

```bash
# عرض معلومات قاعدة البيانات
render database info mnbara-db

# ستحصل على شيء مثل:
# Internal Database URL: postgresql://mnbara_user:PASSWORD@dpg-xxxxx.render.internal:5432/mnbara_prod
```

### الطريقة 3: عبر psql

```bash
# إذا كان لديك psql مثبت
psql postgresql://mnbara_user:PASSWORD@dpg-xxxxx.render.internal:5432/mnbara_prod -c "SELECT 1"
```

---

## 🔴 الحصول على REDIS_URL من Redis

### الطريقة 1: عبر لوحة التحكم (الأسهل)

1. اذهب إلى [Render Dashboard](https://dashboard.render.com)
2. اضغط على **"Redis"** في القائمة اليسرى
3. اختر Redis `mnbara-redis`
4. ستجد صفحة تحتوي على معلومات الاتصال
5. ابحث عن **"Internal Redis URL"** أو **"Connection String"**
6. انسخ الـ URL كاملاً

**الشكل:**
```
redis://default:PASSWORD@dpg-xxxxx.render.internal:6379
```

### الطريقة 2: عبر Render CLI

```bash
# عرض معلومات Redis
render redis info mnbara-redis

# ستحصل على شيء مثل:
# Internal Redis URL: redis://default:PASSWORD@dpg-xxxxx.render.internal:6379
```

### الطريقة 3: عبر redis-cli

```bash
# إذا كان لديك redis-cli مثبت
redis-cli -u redis://default:PASSWORD@dpg-xxxxx.render.internal:6379 ping
```

---

## 📋 خطوات الحصول على القيم (ملخص)

### للـ DATABASE_URL:

1. **اذهب إلى:** Render Dashboard → Databases
2. **اختر:** mnbara-db
3. **ابحث عن:** "Internal Database URL"
4. **انسخ:** القيمة كاملة
5. **مثال:**
   ```
   postgresql://mnbara_user:abc123def456@dpg-c1234567.render.internal:5432/mnbara_prod
   ```

### للـ REDIS_URL:

1. **اذهب إلى:** Render Dashboard → Redis
2. **اختر:** mnbara-redis
3. **ابحث عن:** "Internal Redis URL"
4. **انسخ:** القيمة كاملة
5. **مثال:**
   ```
   redis://default:xyz789uvw012@dpg-c7654321.render.internal:6379
   ```

---

## 🔐 أجزاء الـ URL

### DATABASE_URL

```
postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE
│            │        │        │    │    │
│            │        │        │    │    └─ اسم قاعدة البيانات
│            │        │        │    └────── المنفذ (عادة 5432)
│            │        │        └─────────── اسم المضيف
│            │        └──────────────────── كلمة المرور
│            └───────────────────────────── اسم المستخدم
└──────────────────────────────────────────── نوع قاعدة البيانات
```

**مثال:**
```
postgresql://mnbara_user:abc123@dpg-xxxxx.render.internal:5432/mnbara_prod
            └─────────────┬──────┘ └──────────────┬──────────────┘ └────┬────┘
                    المستخدم:كلمة المرور        المضيف:المنفذ      قاعدة البيانات
```

### REDIS_URL

```
redis://USERNAME:PASSWORD@HOST:PORT
│       │        │        │    │
│       │        │        │    └─ المنفذ (عادة 6379)
│       │        │        └────── اسم المضيف
│       │        └─────────────── كلمة المرور
│       └──────────────────────── اسم المستخدم (عادة "default")
└──────────────────────────────── نوع الخدمة
```

**مثال:**
```
redis://default:xyz789@dpg-xxxxx.render.internal:6379
        └──┬──┘ └──┬──┘ └──────────────┬──────────────┘
        المستخدم كلمة المرور          المضيف:المنفذ
```

---

## ✅ التحقق من الـ URLs

### اختبر DATABASE_URL

```bash
# استبدل القيمة بقيمتك الفعلية
psql "postgresql://mnbara_user:PASSWORD@dpg-xxxxx.render.internal:5432/mnbara_prod" -c "SELECT 1"

# إذا نجح، ستحصل على:
# ?column?
# ----------
#        1
```

### اختبر REDIS_URL

```bash
# استبدل القيمة بقيمتك الفعلية
redis-cli -u "redis://default:PASSWORD@dpg-xxxxx.render.internal:6379" ping

# إذا نجح، ستحصل على:
# PONG
```

---

## 🆘 مشاكل شائعة

### ❌ مشكلة: "Connection refused"

**السبب:** قد تكون الخدمة لم تنته من الإنشاء بعد

**الحل:**
1. انتظر 2-3 دقائق
2. تحقق من أن الخدمة "Running" في لوحة التحكم
3. حاول مرة أخرى

### ❌ مشكلة: "Authentication failed"

**السبب:** كلمة المرور خاطئة

**الحل:**
1. انسخ الـ URL مرة أخرى من لوحة التحكم
2. تأكد من عدم وجود مسافات إضافية
3. تحقق من أن كلمة المرور صحيحة

### ❌ مشكلة: "Host not found"

**السبب:** قد تكون تستخدم External URL بدلاً من Internal URL

**الحل:**
1. استخدم **Internal URL** (يحتوي على `.render.internal`)
2. لا تستخدم External URL (يحتوي على `.c.render.com`)

### ❌ مشكلة: "Connection timeout"

**السبب:** قد تكون الخدمة في منطقة مختلفة

**الحل:**
1. تأكد من أن Database و Redis في نفس المنطقة
2. تأكد من أن Web Service في نفس المنطقة

---

## 📝 نموذج للـ URLs

### DATABASE_URL (مثال حقيقي)

```
postgresql://mnbara_user:a7f3e9c2b1d4f6a8@dpg-c1a2b3c4d5e6f7g8.render.internal:5432/mnbara_prod
```

### REDIS_URL (مثال حقيقي)

```
redis://default:x9y8z7w6v5u4t3s2@dpg-c8g7f6e5d4c3b2a1.render.internal:6379
```

---

## 🎯 الخطوات النهائية

1. ✅ انسخ DATABASE_URL من Render Dashboard
2. ✅ انسخ REDIS_URL من Render Dashboard
3. ✅ اختبر الاتصالات باستخدام الأوامر أعلاه
4. ✅ أضف المتغيرات على Render Dashboard
5. ✅ انشر التطبيق

---

## 📞 روابط مفيدة

- **Render Databases:** https://dashboard.render.com/databases
- **Render Redis:** https://dashboard.render.com/redis
- **PostgreSQL Connection Strings:** https://www.postgresql.org/docs/current/libpq-connect.html
- **Redis Connection Strings:** https://redis.io/docs/clients/

---

**آخر تحديث:** 26 ديسمبر 2025

