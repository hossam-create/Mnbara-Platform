{{- if and .Values.tripsService.enabled .Values.tripsService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.tripsService.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "mnbara.serviceLabels" (list . .Values.tripsService.name) | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.tripsService.name }}
  minReplicas: {{ .Values.tripsService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.tripsService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.tripsService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
