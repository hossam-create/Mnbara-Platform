{{- if .Values.listingService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.listingService.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "mnbarh.serviceLabels" (list . .Values.listingService.name) | nindent 4 }}
spec:
  {{- if not .Values.listingService.autoscaling.enabled }}
  replicas: {{ .Values.listingService.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "mnbarh.serviceSelectorLabels" (list . .Values.listingService.name) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "mnbarh.serviceSelectorLabels" (list . .Values.listingService.name) | nindent 8 }}
    spec:
      serviceAccountName: {{ include "mnbarh.serviceAccountName" . }}
      containers:
        - name: {{ .Values.listingService.name }}
          image: {{ include "mnbarh.image" (dict "root" . "service" .Values.listingService) }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.listingService.service.port }}
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.listingService.env.PORT | quote }}
            - name: NODE_ENV
              value: {{ .Values.listingService.env.NODE_ENV | quote }}
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ default (printf "%s-database" (include "mnbarh.fullname" .)) .Values.secrets.database.existingSecret }}
                  key: password
            - name: DATABASE_URL
              value: {{ include "mnbarh.databaseUrl" . | quote }}
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ default (printf "%s-redis" (include "mnbarh.fullname" .)) .Values.secrets.redis.existingSecret }}
                  key: redis-password
            - name: REDIS_URL
              value: {{ include "mnbarh.redisUrl" . | quote }}
          envFrom:
            - configMapRef:
                name: {{ include "mnbarh.fullname" . }}-common
          resources:
            {{- toYaml .Values.listingService.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
{{- end }}
