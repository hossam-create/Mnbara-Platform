apiVersion: v1
kind: Service
metadata:
  name: { .Release.Name }-user-service
spec:
  type: { .Values.service.type }
  selector:
    app: user-service
  ports:
    - port: { .Values.service.port }
      targetPort: 3000
