feat(frontend): add cart management hook and product card component

- Add TypeScript types for cart and API responses (cart.types.ts)
- Create API service layer with Axios and token authentication
- Implement CartService for backend /api/cart integration
- Add useCart hook with localStorage persistence and backend sync
- Integrate Sentry for automatic error tracking
- Build ProductCard component with Tailwind styling and hover effects
- Add comprehensive unit tests for hook and component (Vitest)

Features:
- Optimistic UI updates with automatic rollback on error
- Automatic cart synchronization on mount (if user authenticated)
- Full test coverage with 22 test cases

AI-assisted: Antigravity
