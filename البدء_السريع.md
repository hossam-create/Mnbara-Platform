# 🚀 البدء السريع - المرحلة 2.5

## ✅ المتطلبات جاهزة
- ✅ Docker يعمل (v29.1.3)
- ✅ Node.js مثبت
- ✅ المكتبات المطلوبة مثبتة (axios, chalk)

---

## 📝 الخطوات التالية

### 1️⃣ تشغيل الخدمات (5 دقائق)

```bash
# تشغيل السكريبت التلقائي
scripts\local-mvp-start.bat
```

أو يدوياً:

```bash
# تشغيل البنية التحتية
docker-compose -f docker-compose.local-mvp.yml up -d postgres redis rabbitmq

# انتظر 30 ثانية
timeout /t 30

# تشغيل الخدمات
docker-compose -f docker-compose.local-mvp.yml up -d
```

---

### 2️⃣ تشغيل Migrations (10 دقائق)

```bash
# Auth Service
cd backend\services\auth-service
npx prisma migrate deploy
cd ..\..\..

# User Service
cd backend\services\user-service
npx prisma migrate deploy
cd ..\..\..

# Wallet Service
cd backend\services\wallet-service
npx prisma migrate deploy
cd ..\..\..

# Payment Service
cd backend\services\payment-service
npx prisma migrate deploy
cd ..\..\..

# Escrow Service
cd backend\services\escrow-service
npx prisma migrate deploy
cd ..\..\..

# Orders Service
cd backend\services\orders-service
npx prisma migrate deploy
cd ..\..\..

# Trips Service
cd backend\services\trips-service
npx prisma migrate deploy
cd ..\..\..

# Matching Service
cd backend\services\matching-service
npx prisma migrate deploy
cd ..\..\..

# Notification Service
cd backend\services\notification-service
npx prisma migrate deploy
cd ..\..\..

# Subscription Service
cd backend\services\subscription-service
npx prisma migrate deploy
cd ..\..\..
```

---

### 3️⃣ إنشاء بيانات الاختبار (2 دقيقة)

```bash
cd scripts
npm run seed
```

سيتم إنشاء:
- 5 مشترين (50,000 جنيه لكل واحد)
- 5 بائعين (10,000 جنيه + اشتراك نشط)
- 5 مسافرين (5,000 جنيه لكل واحد)
- 5 منتجات
- 5 رحلات

---

### 4️⃣ تشغيل الاختبارات (3 دقائق)

```bash
cd scripts
npm test
```

---

## 🔐 بيانات الدخول للاختبار

### مشتري
- البريد: `buyer1@test.local`
- كلمة المرور: `Test123!@#`

### بائع
- البريد: `seller1@test.local`
- كلمة المرور: `Test123!@#`

### مسافر
- البريد: `traveler1@test.local`
- كلمة المرور: `Test123!@#`

---

## 📊 روابط الخدمات

### الخدمات الخلفية
- Auth: http://localhost:3001
- User: http://localhost:3002
- Payment: http://localhost:3003
- Product: http://localhost:3004
- Wallet: http://localhost:3005
- Orders: http://localhost:3006
- Escrow: http://localhost:3007
- Trips: http://localhost:3009
- Matching: http://localhost:3010
- Notification: http://localhost:3011
- Subscription: http://localhost:3012

### البنية التحتية
- PostgreSQL: `localhost:5432`
  - المستخدم: `mnbarh_local`
  - كلمة المرور: `local_test_password`
- Redis: `localhost:6379`
- RabbitMQ: http://localhost:15672
  - المستخدم: `mnbarh_local`
  - كلمة المرور: `local_test_password`

### الواجهة الأمامية
- تطبيق الويب: http://localhost:5173

---

## 🔧 استكشاف الأخطاء

### الخدمات لا تبدأ
```bash
# فحص الحالة
docker-compose -f docker-compose.local-mvp.yml ps

# فحص السجلات
docker-compose -f docker-compose.local-mvp.yml logs -f [service-name]

# إعادة تشغيل
docker-compose -f docker-compose.local-mvp.yml restart [service-name]
```

### تنظيف كامل
```bash
# إيقاف كل شيء
docker-compose -f docker-compose.local-mvp.yml down

# حذف البيانات (تحذير!)
docker-compose -f docker-compose.local-mvp.yml down -v

# بداية جديدة
docker-compose -f docker-compose.local-mvp.yml up -d
```

---

## ⏱️ الوقت المتوقع

- تشغيل الخدمات: 5 دقائق
- Migrations: 10 دقائق
- إنشاء البيانات: 2 دقيقة
- الاختبارات: 3 دقيقة

**المجموع: ~20 دقيقة**

---

## 📝 بعد الانتهاء

1. افتح `PHASE_2_6_VALIDATION_REPORT_TEMPLATE.md`
2. انسخه إلى `PHASE_2_6_VALIDATION_REPORT.md`
3. املأ النتائج
4. وثق المشاكل
5. أرسل التقرير

---

## 🚫 تذكير مهم

- ❌ لا تستخدم بيانات حقيقية
- ❌ لا تستخدم Stripe حقيقي
- ❌ لا تنشر على السحابة
- ✅ اختبار محلي فقط

---

**🔒 وضع الاختبار فقط - لا بيانات إنتاج**

